<!DOCTYPE html>
<html lang="ja">

<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>メニュー（FIN008/mypage）</title>

    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="fin008">

    <div class="container fin008-container">

        <% if (message) { %>
            <p class="success-message"
                style="color: green; padding: 10px; border: 1px solid green; background-color: #e9ffe9; margin-bottom: 20px;">
                ✅ <%= message %>
            </p>
            <% } %>

                <a href="/changeIcon" class="fin008-photo-area">
                    <img src="<%= userIconUrl %>" alt="ユーザーアイコン" class="fin008-icon">
                </a>

                <div class="fin008-username">
                    <%= userName %>
                </div>

                <div class="fin008-menu-links">
                    <a href="/preference?returnUrl=<%= encodeURIComponent('/mypage?returnUrl=' + encodeURIComponent(backUrl)) %>" class="btn fin008-btn">個人設定</a>
                    <a href="/favorite?returnUrl=<%= encodeURIComponent('/mypage?returnUrl=' + encodeURIComponent(backUrl)) %>" class="btn fin008-btn">お気に入り</a>
                    <a href="/FIN017?returnUrl=<%= encodeURIComponent('/mypage?returnUrl=' + encodeURIComponent(backUrl)) %>" class="btn fin008-btn">フレンド</a>
                    <a href="<%= backUrl %>" class="btn fin008-btn">
                        戻る
                    </a>
                </div>


    </div>

    <script src="/js/fin008.js"></script>
</body>

</html>