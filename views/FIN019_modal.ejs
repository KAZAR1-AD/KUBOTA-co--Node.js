<!DOCTYPE html>
<html lang="ja">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= pageTitle %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        // 汎用的な名前に変更
                        'primary': '#007bff', // funabashi-blue
                        'secondary': '#28a745', // funabashi-green
                        'background-light': '#f8f9fa', // funabashi-light
                    }
                }
            }
        }
    </script>
    <style>
        /* アイコン選択時のカスタムスタイル（前回のコードから変更なし） */
        .icon-item input[type="radio"]:checked+label {
            box-shadow: 0 0 0 4px #007bff, 0 0 0 6px white;
            transform: scale(1.05);
        }

        .icon-item input[type="radio"] {
            position: absolute;
            opacity: 0;
            width: 0;
            height: 0;
        }

        .icon-label {
            transition: transform 0.1s, box-shadow 0.1s;
            cursor: pointer;
            display: block;
            border-radius: 9999px;
            overflow: hidden;
            border: 2px solid transparent;
        }

        .icon-label:hover {
            opacity: 0.85;
            transform: scale(1.02);
        }
    </style>
</head>

<body class="bg-background-light min-h-screen flex flex-col items-center py-10">

    <div class="w-full max-w-xl mx-auto p-6 bg-white shadow-xl rounded-xl">
        <h2 class="text-3xl font-bold text-center text-gray-800 mb-6 border-b pb-4">
            プロフィール画像の変更
        </h2>

        <% if (error) { %>
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
                <p>
                    <%= error %>
                </p>
            </div>
            <% } %>
                <% if (message) { %>
                    <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4"
                        role="alert">
                        <p>
                            <%= message %>
                        </p>
                    </div>
                    <% } %>

                        <div class="flex flex-col items-center mb-8 p-4 border rounded-lg bg-gray-50">
                            <p class="text-lg font-semibold text-gray-600 mb-2">現在のプロフィール</p>
                            <img src="<%= userIconUrl %>" alt="現在のアイコン"
                                class="w-20 h-20 rounded-full object-cover ring-2 ring-primary mb-2">
                            <p class="text-xl font-bold text-gray-800">
                                <%= userName %>
                            </p>
                            
                        </div>

                        <form action="/update-profile-photo" method="POST" id="icon-form">

                            <div class="grid grid-cols-4 gap-4 p-4 border rounded-lg bg-white shadow-inner mb-6">
                                <% if (availableIcons && availableIcons.length> 0) { %>
                                    <% availableIcons.forEach(icon=> { %>
                                        <div class="icon-item flex justify-center items-center p-1">
                                            <input type="radio" id="icon_<%= icon.profile_photo_id %>" name="newPhotoId"
                                                value="<%= icon.profile_photo_id %>"
                                                <%= profilePhotoId == icon.profile_photo_id ? 'checked' : '' %>>
                                            <label for="icon_<%= icon.profile_photo_id %>" class="icon-label w-16 h-16">
                                                <img src="<%= icon.photo_address %>" alt="アイコン"
                                                    class="w-full h-full object-cover">
                                            </label>
                                        </div>
                                    <% }); %>
                                <% } else { %>
                                    <div class="col-span-4 text-center py-4 text-gray-500">
                                        選択可能なアイコンがありません。
                                    </div>
                                <% } %>
                            </div>

                            <div class="flex justify-between items-center space-x-4 pt-4 border-t">
                                <button type="button" onclick="history.back()"
                                    class="px-4 py-2 bg-gray-500 text-white font-semibold rounded-lg hover:bg-gray-600 transition duration-150 shadow-md flex-1">
                                    メニューに戻る
                                </button>
                                <button type="submit"
                                    class="px-4 py-2 bg-primary text-white font-semibold rounded-lg hover:bg-secondary transition duration-150 shadow-md flex-1">
                                    選択した画像で保存
                                </button>
                            </div>
                        </form>

    </div>
</body>

</html>