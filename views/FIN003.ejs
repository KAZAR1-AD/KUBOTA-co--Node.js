<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新規登録（FIN003）</title>
    <!-- 外部CSSファイル（style.css）を読み込み -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="font-sans">
    <div class="container">
        <h1 class="fin003-title">新規登録</h1>
        
        <!-- EJSテンプレートによるサーバーサイドエラー表示エリア -->
        <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
            <p class="error-message">
                <%= error %>
            </p>
        <% } %>

        <form action="/register-confirm" method="POST">
            
            <div class="form-group">
                <label for="username" class="form-label">ユーザー名</label>
                <input type="text" id="username" name="username" class="form-input" required>
            </div>

            <div class="form-group">
                <label for="email" class="form-label">メールアドレス</label>
                <input type="email" id="email" name="email" class="form-input" required>
            </div>

            <div class="form-group">
                <label for="password" class="form-label">パスワード</label>
                <!-- 最小文字数 (8文字) を設定 -->
                <input type="password" id="password" name="password" class="form-input" required minlength="8">
                <p class="password-hint">※ 8文字以上で入力してください。</p>
            </div>

            <div class="form-group" style="margin-bottom: 1.5rem;">
                <label for="confirm_password_field" class="form-label">パスワード（確認用）</label>
                <!-- 最小文字数 (8文字) を設定 -->
                <input type="password" id="confirm_password_field" name="confirm_password" class="form-input" required minlength="8">
            </div>

            <div class="button-area">
                <button type="submit" class="btn btn-primary">
                    登録する
                </button>
                
                <a href="/FIN002" class="btn btn-secondary" role="button">
                    戻る
                </a>
            </div>
        </form>
    </div>
</body>
</html>